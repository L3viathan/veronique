from datetime import timedelta


SESSION_REFRESH_AFTER = timedelta(days=7)
SESSION_MAX_AGE = timedelta(days=30)

CLAIM_DATA_CACHE_TIME = timedelta(minutes=5)

SEARCH_AVGDL_CACHE_TIME = timedelta(hours=1)
