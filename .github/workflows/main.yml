name: CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: Set up Python
        uses: actions/setup-python@main
        with:
          python-version: "3.9"
      - name: Install dependencies
        run: python -m pip install --upgrade pip sanic pytest ruff sanic-testing pytest-asyncio markdown-it-py[linkify] phonenumbers
      - name: Test
        run: python -m pytest
      - name: Lint
        run: ruff check
    permissions:
      contents: read
